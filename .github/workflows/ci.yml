name: "Check Rust code"

on:
  push:
    branches:
    - main
    paths:
    - '**.rs'
  pull_request:
    branches:
    - "main"
    paths:
    - '**.rs'
    
env:
  CARGO_TERM_COLOR: always
  # Disable incremental compilation for faster from-scratch builds
  CARGO_INCREMENTAL: 0

jobs:

  check_rust:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout directory
        uses: actions/checkout@v6
        
      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Cargo check
        run: cargo check --locked

  test_rust:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout directory
        uses: actions/checkout@v6
        
      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2
      
      - name: Cargo test
        run: cargo test --locked

  test_zip:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout directory
        uses: actions/checkout@v6
        
      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2
      
      - name: Create sample WACZ collections
        run: |
          cargo run tests/example.warc.gz --output tests/output_single_warc.wacz
          cargo run tests/example.warc.gz tests/example2.warc.gz --output tests/output_multiple_warcs.wacz

      - name: Validate wacz file with unzip
        run: |
          unzip -tv tests/output_single_warc.wacz
          unzip -tv tests/output_multiple_warcs.wacz
          

  test_wacz:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout directory
        uses: actions/checkout@v6
        
      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2
      
      - name: Create sample WACZ collections
        run: |
          cargo run tests/example.warc.gz --output tests/output_single_warc.wacz
          cargo run tests/example.warc.gz tests/example2.warc.gz --output tests/output_multiple_warcs.wacz

      - name: Setup py-wacz
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'
          cache: 'pip' 
      - run: pip install -r tests/requirements.txt

      - name: Validate wacz file with py-wacz
        run: |
          wacz validate -f tests/output_single_warc.wacz
          wacz validate -f tests/output_multiple_warcs.wacz
