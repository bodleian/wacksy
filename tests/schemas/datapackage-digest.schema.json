{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "tests/schemas/datapackage-digest.schema.json",
    "description": "A simple piece of JSON to verify the datapackage.json manifest with a hash.",
    "type": "object",
    "properties": {
        "path": {
            "type": "string",
            "description": "The path of the datapackage file, should just be 'datapackage.json'.",
            "pattern": "^datapackage.json$"
        },
        "hash": {
            "type": "string",
            "description": "A hash of the datapackage file, should match the regex here but if it doesn't please change it!",
            "pattern": "^[a-z]+([0-9][0-9][0-9]|512):[a-fA-F0-9]{32,128}$"
        }
    },
    "required": [
        "path",
        "hash"
    ]
}